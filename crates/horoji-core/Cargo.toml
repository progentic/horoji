[package]
name = "horoji-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Core acceptability engineering authority types for Horoji."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
proptest = "1"
